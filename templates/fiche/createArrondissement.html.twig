{% extends 'base.html.twig' %}

{% form_theme formArrondissement 'bootstrap_4_layout.html.twig' %}

{% block body %}

    {% if app.user %}

    <h1>Creation Arrondissement</h1>

    {{ form_start(formArrondissement) }}

    {{ form_widget(formArrondissement) }}
    <button type="submit" class="btn btn-success">Ajouter l'arrondissement</button>

    {{ form_end(formArrondissement) }}

        <a href="{{ path('arrondissement_index') }}">retour à la liste</a>

    {% else %}
        <h2>Veuillez vous connecter</h2>
        <a href="/connexion" class="btn btn-primary">
            Connexion
        </a>

    {% endif %}





{% endblock %}