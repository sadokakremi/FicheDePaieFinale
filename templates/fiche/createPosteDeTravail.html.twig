{% extends 'base.html.twig' %}

{% form_theme formPosteDeTravail 'bootstrap_4_layout.html.twig' %}

{% block body %}
    {% if app.user %}

    <h1>Definir Poste De Travail</h1>

    {{ form_start(formPosteDeTravail) }}

    {{ form_widget(formPosteDeTravail) }}
    <button type="submit" class="btn btn-success">Valider Poste De Travail</button>

    {{ form_end(formPosteDeTravail) }}
    {% else %}
        <h2>Veuillez vous connecter</h2>
        <a href="/connexion" class="btn btn-primary">
            Connexion
        </a>

    {% endif %}
    <a href="{{ path('posteDeTravail_index') }}">retour à la liste</a>



{% endblock %}