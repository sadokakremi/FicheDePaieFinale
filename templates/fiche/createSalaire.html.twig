{% extends 'base.html.twig' %}

{% form_theme formSalaire 'bootstrap_4_layout.html.twig' %}

{% block body %}

    {% if app.user %}

    <h1>Definir Salaire</h1>

    {{ form_start(formSalaire) }}

    {{ form_widget(formSalaire) }}
    <button type="submit" class="btn btn-success">Valider Salaire</button>

    {{ form_end(formSalaire) }}

    {% else %}
        <h2>Veuillez vous connecter</h2>
        <a href="/connexion" class="btn btn-primary">
            Connexion
        </a>

     {% endif %}
    <a href="{{ path('salaire_index') }}">retour à la liste</a>



{% endblock %}